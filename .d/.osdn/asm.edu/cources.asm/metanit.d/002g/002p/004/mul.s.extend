# Первая программа
.globl _start
 
.section .text
_start:
    movq $0xC000000000000002, %rdi
    movq $4, %rax
    # mulq :: allways RAX : %rax*%rdi->%rax
    mulq %rdi 
    # %rax->%rdi as return num
    movq %rax, %rdi
    movq $60, %rax
    syscall
